<Window x:Class="AchievementOverlay.NotificationWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        Topmost="True"
        ShowInTaskbar="False"
        ShowActivated="False"
        SizeToContent="WidthAndHeight"
        RenderTransformOrigin="0.5,1">
    <Window.RenderTransform>
        <TranslateTransform x:Name="SlideTransform" Y="0" />
    </Window.RenderTransform>
    <Border x:Name="RootBorder"
            Background="#DD1A1A2E"
            CornerRadius="8"
            Padding="12">
        <!-- The whole popup is laid out at a fixed design size and scaled uniformly so the
             font, icon, spacing and text-wrap width always keep the same proportions. -->
        <Border.LayoutTransform>
            <ScaleTransform x:Name="RootScale" ScaleX="1" ScaleY="1" />
        </Border.LayoutTransform>
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>
            <Image x:Name="AchievementIcon"
                   Grid.Column="0"
                   Width="56"
                   Height="56"
                   Stretch="UniformToFill"
                   VerticalAlignment="Top"
                   Margin="0,0,12,0" />
            <DockPanel Grid.Column="1"
                       VerticalAlignment="Stretch">
                <TextBlock x:Name="GameInfoText"
                           DockPanel.Dock="Bottom"
                           Foreground="#99AAAAAA"
                           FontSize="10"
                           Width="230"
                           TextWrapping="Wrap"
                           Margin="0,4,0,0"
                           Visibility="Collapsed" />
                <StackPanel DockPanel.Dock="Top"
                            VerticalAlignment="Top">
                    <TextBlock x:Name="AchievementName"
                               Foreground="White"
                               FontWeight="Bold"
                               FontSize="14"
                               Width="230"
                               TextWrapping="Wrap" />
                    <TextBlock x:Name="AchievementDescription"
                               Foreground="#CCAAAAAA"
                               FontSize="12"
                               Width="230"
                               TextWrapping="Wrap"
                               Margin="0,2,0,0" />
                </StackPanel>
            </DockPanel>
        </Grid>
    </Border>
</Window>
